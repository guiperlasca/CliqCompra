package com.trabalho.cliqaqui.model;

public enum StatusPagamento {
    PENDENTE, // O pagamento ainda não foi processado
    QUITADO,  // O pagamento foi confirmado
    CANCELADO // O pagamento foi cancelado
}